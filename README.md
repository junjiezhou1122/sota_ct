# sota_ct

Python utilities for exploring the RadGenome Chest CT dataset.

## Environment

- Install dependencies with Poetry: `poetry install`
- Run scripts through Poetry, e.g. `poetry run python datasets_explore/examine_csv.py`
- Add new libraries with `poetry add <package>`; optional tooling can go in a dev group via `poetry add --group dev <package>`
